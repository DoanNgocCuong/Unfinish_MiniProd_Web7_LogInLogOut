import { Box } from "@chakra-ui/react";

const BuildConversation = () => {
    return (
        <Box mt={40}>
            <h1>BuildConversation</h1>
        </Box>
    )
}

export default BuildConversation;